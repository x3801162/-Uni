import { ALPHA_FORMAT_MAP, FORMATS } from './constants';
export declare type BasicColorFormat = typeof FORMATS[number];
export declare type AlphaConvertibleFormat = keyof typeof ALPHA_FORMAT_MAP;
export declare type AlphaColorFormat = typeof ALPHA_FORMAT_MAP[AlphaConvertibleFormat];
export declare type ColorFormat = BasicColorFormat | AlphaColorFormat;
export interface ColorInputProp {
    key: string;
    min?: number;
    max?: number;
    type: 'input' | 'inputNumber';
    flex?: number;
    format?: Function;
}
