import { PickerColumn } from './type';
import { KeysType } from '../common';
export declare const isMultipleArray: (arr: PickerColumn | PickerColumn[]) => boolean;
export declare const getPickerColumns: (columns: any) => PickerColumn[];
export declare const limitNumberInRange: (num: number, min: number, max: number) => number;
export declare function findIndexOfEnabledOption(options: PickerColumn, startIndex: number, keys?: KeysType): number;
